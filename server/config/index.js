const config = {
  appName: 'message-timeline',
  host: '127.0.0.1',
  port: 3000,
  database: {
    host: 'localhost',
    db: 'message-timeline'
  },
  permission: false // 访权问限
}

module.exports = config

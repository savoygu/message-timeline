{
  "name": "message-timeline",
  "version": "1.0.0",
  "description": "留言时间轴，记录美好时刻",
  "author": "savoygu",
  "private": true,
  "scripts": {
    "dev:client": "npm run start --prefix client",
    "dev:server": "npm run start --prefix server",
    "start": "npm-run-all -p dev:*",
    "build:client": "npm run build --prefix client"
  },
  "devDependencies": {
    "npm-run-all": "^4.1.2"
  },
  "engines": {
    "node": ">= 4.0.0",
    "npm": ">= 3.0.0"
  },
  "dependencies": {
    "md5": "^2.3.0"
  }
}
